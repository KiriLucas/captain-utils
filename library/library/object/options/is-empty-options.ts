export class IsEmptyOptions {
  /**
   * If true, won't throw an exception when an invalid input type is used.
   * This may lead to unexpected behaviours.
   */
  ignoreTypeValidation: boolean = false;
}
